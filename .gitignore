.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Real catalogue data. These carry a shop's titles, SKUs, prices, image URLs
# and sometimes customer-facing copy -- none of it belongs in git.
*.csv
*.xlsx
output/
images/

# Local configuration and the IP denylist. config.example.yaml is committed;
# the real one never is.
config.yaml
denylist.txt

# Job state and driver binaries.
*_progress_*.csv
*_cache.json
chromedriver.exe
.env
